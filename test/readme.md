combining doc and testing: develop in `marimo` to create docs but mark testing cells to run `pytest`.

process:
1. `cd` into [this dir](.).
2. Use `marimo` to edit notebooks.
3. Run `pytest`. regression testing data lives in [regression](./regression).