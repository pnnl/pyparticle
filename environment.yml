name: pyparticle3
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - numpy
  - scipy
  - matplotlib
  - pyprojroot
  - pyyaml
  - pip
  # Uncomment if you want to install pytest via conda:
  # - pytest
  # - pytest-cov
  - pip:
      - pytest
      - pytest-cov
      - codecov
      - pymiescatt
      - pyBCabs
      - pyparticle
